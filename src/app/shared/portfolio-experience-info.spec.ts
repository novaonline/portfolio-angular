import { PortfolioExperienceInfo } from './portfolio-experience-info';

describe('PortfolioExperienceInfo', () => {
  it('should create an instance', () => {
    expect(new PortfolioExperienceInfo()).toBeTruthy();
  });
});
