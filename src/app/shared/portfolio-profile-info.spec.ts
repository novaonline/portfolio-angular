import { PortfolioProfileInfo } from './portfolio-profile-info';

describe('PortfolioProfileInfo', () => {
  it('should create an instance', () => {
    expect(new PortfolioProfileInfo()).toBeTruthy();
  });
});
