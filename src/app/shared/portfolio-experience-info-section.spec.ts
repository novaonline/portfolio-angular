import { PortfolioExperienceInfoSection } from './portfolio-experience-info-section';

describe('PortfolioExperienceInfoSection', () => {
  it('should create an instance', () => {
    expect(new PortfolioExperienceInfoSection()).toBeTruthy();
  });
});
