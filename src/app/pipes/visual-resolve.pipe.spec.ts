import { VisualResolvePipe } from './visual-resolve.pipe';

describe('VisualResolvePipe', () => {
  it('create an instance', () => {
    const pipe = new VisualResolvePipe();
    expect(pipe).toBeTruthy();
  });
});
